# {{ ansible_managed }}
# Rotate MongoDB logs
{{ mongod-config_log_dir }}/*.log {
    daily
    rotate {{ mongdb_days_of_logs_to_keep }}
    compress
    dateext
    missingok
    notifempty
    sharedscripts
    copytruncate
    postrotate
        /bin/kill -SIGUSR1 $(cat {{ mongod-config_pid_file }} 2> /dev/null) 2> /dev/null || true
    endscript
}
